import Login from "../components/auth/Login";

const LoginPage = () => {
  return (
    <div className="flex items-center justify-center bg-login-bg h-screen">
      <Login />
    </div>
  );
};

export default LoginPage;
